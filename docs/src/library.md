# Library reference

Everything exported by the course library. This page is generated from the
docstrings in `code/src`, so it stays in sync with the code automatically.

```@index
```

```@autodocs
Modules = [VLDataScienceMachineLearningPackage]
Order = [:type, :function, :constant, :macro]
```
