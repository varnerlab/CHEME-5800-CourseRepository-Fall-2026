# Voice calibration: L6a urea-cycle flux balance example

`before.ipynb` is the example notebook as committed in `1280c5a` (2026-09-25),
copied before the instructor's manual edits to
`weeks/week-06/L6a/CHEME-5800-L6a-Example-UreaCycle-FluxBalance-Fall-2026.ipynb`.
The instructor had already made some edits to the example before this commit;
those are part of the baseline, not the round being measured.

After the edits, compare the two versions cell by cell and record the concrete
lessons in the shared notebook style guide.
